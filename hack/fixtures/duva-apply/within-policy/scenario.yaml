# The newer versions duva should find in this scenario.
#
# The one thing the compose file cannot say, since no service references
# them. Everything it does name is published from the file itself.
updates:
  - 1.0.1
