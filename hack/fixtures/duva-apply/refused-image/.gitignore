# duva's state and the registry's certificate live inside the project so the
# compose file can name them relatively. They are not part of the stack, and
# duva refuses to act on a repository with uncommitted changes.
/data/
/certs/
